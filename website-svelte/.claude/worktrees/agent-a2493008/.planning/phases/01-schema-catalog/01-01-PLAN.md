---
phase: 01-schema-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/convex/schema.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07

must_haves:
  truths:
    - "Convex schema defines all 5 domain tables: catalogItems, diddlTypes, lists, listItems, userProfiles"
    - "catalogItems has compound index by_type_number on (type, number)"
    - "catalogItems.imageStorageIds is an array of storage IDs (future-ready)"
    - "shadcn-svelte is initialized with stone theme and components.json exists"
    - "runed package is installed"
    - "accordion, badge, card shadcn components are added"
    - "Generated Convex API is up to date with new schema"
  artifacts:
    - path: "src/convex/schema.ts"
      provides: "Full domain schema with all 5 tables and compound index"
      contains: "by_type_number"
    - path: "components.json"
      provides: "shadcn-svelte initialization marker"
    - path: "src/lib/components/ui/accordion"
      provides: "shadcn accordion component"
    - path: "src/lib/components/ui/badge"
      provides: "shadcn badge component"
    - path: "src/lib/components/ui/card"
      provides: "shadcn card component"
    - path: "src/convex/_generated/api.d.ts"
      provides: "Generated Convex API types (regenerated after schema change)"
  key_links:
    - from: "src/convex/schema.ts"
      to: "src/convex/_generated/api.d.ts"
      via: "bun run convex:gen"
      pattern: "catalogItems"
---

<objective>
Initialize the design system tooling and replace the placeholder Convex schema with the full Diddl domain model.

Purpose: Every subsequent plan depends on (a) the schema existing with the compound index, and (b) shadcn-svelte components being available for import. Both must be done in Wave 1 before any other code is written.

Output:
- `src/convex/schema.ts` — full domain schema (catalogItems, diddlTypes, lists, listItems, userProfiles)
- `components.json` — shadcn-svelte initialized with stone theme
- `src/lib/components/ui/accordion`, `badge`, `card` — shadcn component files
- `src/convex/_generated/api.d.ts` — regenerated with new schema types
- `runed` added to dependencies
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-catalog/01-RESEARCH.md
@.planning/phases/01-schema-catalog/01-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Current schema (to be replaced): -->
From src/convex/schema.ts (CURRENT — placeholder, will be replaced):
```typescript
import { defineSchema, defineTable } from 'convex/server';
import { v } from 'convex/values';

export default defineSchema({
  conferences: defineTable({
    name: v.string(),
    location: v.string(),
    startDate: v.number(),
    endDate: v.number(),
    description: v.optional(v.string())
  })
});
```

<!-- Target schema shape (from RESEARCH.md Pattern 1): -->
Target: 5 tables — catalogItems, diddlTypes, lists, listItems, userProfiles
catalogItems has: type, number, name?, edition?, releaseDate?, imageStorageIds[], imagePath?
catalogItems index: .index('by_type_number', ['type', 'number'])
diddlTypes has: slug, displayName, sortOrder — indexes: by_slug, by_sort_order
lists has: name, description?, color, ownerSubject — index: by_owner
listItems has: listId, catalogItemId, condition (union), quantity, complete, tags[] — indexes: by_list, by_list_catalog
userProfiles has: ownerSubject, name?, bio?, hobbies[], pictureStorageId? — index: by_owner
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install runed and initialize shadcn-svelte with stone theme</name>
  <files>package.json, bun.lock, components.json, src/lib/components/ui/accordion, src/lib/components/ui/badge, src/lib/components/ui/card</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/package.json (confirm runed and shadcn-svelte are not yet installed)
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/01-schema-catalog/01-RESEARCH.md (installation sequence in Standard Stack section)
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/01-schema-catalog/01-UI-SPEC.md (design system section for stone theme confirmation)
  </read_first>
  <action>
Run the following commands in order from the website-svelte directory. Do NOT skip or reorder.

1. Install runed:
```
bun add runed
```

2. Initialize shadcn-svelte. This is an interactive CLI — when prompted, select:
   - Style: default
   - Base color: stone
   - CSS variables: yes
   - All other prompts: accept defaults
```
npx shadcn-svelte@latest init
```

3. After init completes and components.json exists, add the three required components:
```
npx shadcn-svelte@latest add accordion badge card
```

Do not write any component code before step 3 completes. The `$lib/components/ui/accordion`, `$lib/components/ui/badge`, and `$lib/components/ui/card` import paths will not resolve until the CLI generates those files.

After all three steps: run `bun run lint` and fix any new lint errors introduced by shadcn-svelte's generated files. Run `bun run check` to confirm TypeScript passes.
  </action>
  <verify>
    <automated>ls /home/btam/other-code/diddl-manager/website-svelte/components.json && ls /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/accordion && ls /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/badge && ls /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/card && grep '"runed"' /home/btam/other-code/diddl-manager/website-svelte/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists at the project root (website-svelte/)
    - `components.json` contains `"style": "default"` and a base color referencing stone (grep: `"baseColor": "stone"` or `stone` in the color field)
    - `src/lib/components/ui/accordion/` directory exists and is non-empty
    - `src/lib/components/ui/badge/` directory exists and is non-empty
    - `src/lib/components/ui/card/` directory exists and is non-empty
    - `package.json` contains `"runed"` in dependencies
    - `bun run check` exits with 0 errors
  </acceptance_criteria>
  <done>shadcn-svelte is initialized with stone theme, accordion/badge/card components generated, runed installed — all confirmed by file existence and package.json check.</done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder schema with full Diddl domain model</name>
  <files>src/convex/schema.ts</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/src/convex/schema.ts (current state — the conferences placeholder)
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/01-schema-catalog/01-RESEARCH.md (Pattern 1: Convex Schema with Compound Index — exact schema definition)
    - /home/btam/other-code/diddl-manager/website-svelte/src/convex/authed/conferences.ts (existing authed functions referencing old schema — will break, note for cleanup)
  </read_first>
  <action>
Replace the entire contents of `src/convex/schema.ts` with the following. This completely replaces the `conferences` placeholder table with the five Diddl domain tables.

```typescript
import { defineSchema, defineTable } from 'convex/server';
import { v } from 'convex/values';

export default defineSchema({
  catalogItems: defineTable({
    type: v.string(),           // "A4", "sticker", etc. — matches diddlTypes.slug
    number: v.number(),         // sequential integer extracted from filename
    name: v.optional(v.string()),       // human-readable display name
    edition: v.optional(v.string()),    // optional — not present in catalog.json
    releaseDate: v.optional(v.number()), // optional — epoch ms; not in catalog.json
    imageStorageIds: v.array(v.id('_storage')), // DATA-07: array, future-ready
    imagePath: v.optional(v.string()),  // raw path from catalog.json for reference
  }).index('by_type_number', ['type', 'number']), // DATA-06: MUST exist before any catalog query

  diddlTypes: defineTable({     // DATA-05: managed collection, not hardcoded enum
    slug: v.string(),           // "A4", "sticker", etc. — matches catalogItems.type
    displayName: v.string(),    // "A4 Sheets", "Stickers", etc.
    sortOrder: v.number(),      // controls sidebar display order
  })
    .index('by_slug', ['slug'])
    .index('by_sort_order', ['sortOrder']),

  lists: defineTable({          // DATA-02: declared now, used in Phase 2
    name: v.string(),
    description: v.optional(v.string()),
    color: v.string(),
    ownerSubject: v.string(),   // Clerk identity.subject — NOT email
  }).index('by_owner', ['ownerSubject']),

  listItems: defineTable({      // DATA-03: declared now, used in Phase 2
    listId: v.id('lists'),
    catalogItemId: v.id('catalogItems'),
    condition: v.union(
      v.literal('mint'),
      v.literal('near_mint'),
      v.literal('good'),
      v.literal('poor'),
      v.literal('damaged')
    ),
    quantity: v.number(),
    complete: v.boolean(),
    tags: v.array(v.string()),
  })
    .index('by_list', ['listId'])
    .index('by_list_catalog', ['listId', 'catalogItemId']),

  userProfiles: defineTable({   // DATA-04: declared now, used in Phase 4
    ownerSubject: v.string(),
    name: v.optional(v.string()),
    bio: v.optional(v.string()),
    hobbies: v.array(v.string()),
    pictureStorageId: v.optional(v.id('_storage')),
  }).index('by_owner', ['ownerSubject']),
});
```

After writing the file, run:
```
bun run convex:gen
```

This regenerates `src/convex/_generated/api.d.ts` and related files. The existing `src/convex/authed/conferences.ts` will now have type errors because the `conferences` table no longer exists. Delete `src/convex/authed/conferences.ts` (the placeholder — it will be replaced by catalog.ts in Plan 02). Also update `src/routes/app/+page.svelte` to remove any `useQuery(api.authed.conferences.list, {})` calls — replace the content with a minimal placeholder that renders "Diddl Manager — Phase 1 in progress" so the page compiles without errors.

After `convex:gen`, run `bun run lint`, `bun run format`, and `bun run check`. Fix all errors.
  </action>
  <verify>
    <automated>grep -n "by_type_number" /home/btam/other-code/diddl-manager/website-svelte/src/convex/schema.ts && grep -n "imageStorageIds" /home/btam/other-code/diddl-manager/website-svelte/src/convex/schema.ts && grep -n "diddlTypes" /home/btam/other-code/diddl-manager/website-svelte/src/convex/schema.ts && grep -n "listItems" /home/btam/other-code/diddl-manager/website-svelte/src/convex/schema.ts && grep -n "userProfiles" /home/btam/other-code/diddl-manager/website-svelte/src/convex/schema.ts && grep -n "catalogItems" /home/btam/other-code/diddl-manager/website-svelte/src/convex/_generated/api.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/convex/schema.ts` contains `by_type_number` (grep matches)
    - `src/convex/schema.ts` contains `imageStorageIds: v.array(v.id('_storage'))` (grep matches `imageStorageIds`)
    - `src/convex/schema.ts` contains all 5 table names: `catalogItems`, `diddlTypes`, `lists`, `listItems`, `userProfiles`
    - `src/convex/_generated/api.d.ts` references `catalogItems` (confirms convex:gen ran successfully)
    - `bun run check` exits with 0 errors (no TypeScript errors from schema change)
    - `src/convex/authed/conferences.ts` is deleted (the conferences placeholder no longer exists)
  </acceptance_criteria>
  <done>Full 5-table domain schema in place with compound index; generated API up to date; codebase compiles clean.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `components.json` exists with stone base color
2. `src/lib/components/ui/accordion/`, `badge/`, `card/` directories are non-empty
3. `package.json` contains `runed` in dependencies
4. `src/convex/schema.ts` contains `by_type_number`, `imageStorageIds`, and all 5 table names
5. `src/convex/_generated/api.d.ts` references `catalogItems`
6. `bun run check` exits 0
</verification>

<success_criteria>
- shadcn-svelte initialized (stone theme, components.json exists)
- accordion, badge, card components available at `$lib/components/ui/*`
- runed available at `import { useIntersectionObserver } from 'runed'`
- All 5 domain tables defined in schema.ts with correct field types
- Compound index `by_type_number` on `['type', 'number']` defined in catalogItems
- `imageStorageIds: v.array(v.id('_storage'))` declared (DATA-07 future-ready)
- `bun run convex:gen` completed, generated API reflects new schema
- No TypeScript errors (`bun run check` passes)
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-catalog/01-01-SUMMARY.md` using the summary template.
</output>
