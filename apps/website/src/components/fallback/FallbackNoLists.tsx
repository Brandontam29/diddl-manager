import FallbackMessage from "./FallbackMessage";

const FallbackNoLists = () => (
  <FallbackMessage title="No lists yet!">
    You can create one by pressing the button above. Then select Diddls by Diddl Type and press Add
    to List on the top bar.
  </FallbackMessage>
);

export default FallbackNoLists;
