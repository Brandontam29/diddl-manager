# Scaffold the apps/website workspace

Type: task
Status: open
Blocked by: — (none; frontier)

## Question

Create `apps/website` (`@diddl/website`) per spec.md §2 and §9: TanStack Start (Solid) on `solid-js ^1.9.15` / `@tanstack/solid-start ^1.168.46` / `@tanstack/solid-router ^1.170.29`, Tailwind 4 via `@tailwindcss/vite`, Vitest (`test`, `test:unit`, `test:integration`, `passWithNoTests`), `lint`/`typecheck`/`format` scripts using the root oxlint/oxfmt configs, `.env.example` with the five env vars, `src/server/logger.ts`, a placeholder `/` route. Root `package.json`: add the five `*:website` scripts, remove the stale `*:website-svelte` scripts and trustedDependencies entry. Add `.github/workflows/ci.yaml` (website-only: format:check → lint → typecheck → unit tests → build; the db:migrate + integration steps are wired in 'Server functions and the scoping suite' once secrets exist). Update `.oxlintrc.json` ignore to `apps/website/data/catalog.json`.

Done when: `bun run dev:website` serves the placeholder, `bun run build:website`, `lint:website`, `typecheck:website`, `test:website` all pass locally, and ci.yaml is green on main.

## Answer

**2026-08-21 — done.** `apps/website` (`@diddl/website`) exists and every listed
check passes locally.

What was created:

- `apps/website/package.json` — deps `solid-js ^1.9.15`, `@tanstack/solid-start
^1.168.46`, `@tanstack/solid-router ^1.170.29`; dev deps `vite ^8`,
  `vite-plugin-solid`, `nitro` (3.0 beta, the Vercel/Node adapter TanStack Start
  uses), `tailwindcss` + `@tailwindcss/vite` 4, `vitest ^4.1`, `typescript ^6`.
  Scripts exactly as spec §9: `dev`, `build`, `lint`, `typecheck`, `format`, `test`,
  `test:unit`, `test:integration` (both `passWithNoTests`). `db:generate` /
  `db:migrate` are deliberately left to "Drizzle schema and first migration" —
  they need drizzle-kit and `drizzle.config.ts`.
- `vite.config.ts` (`tailwindcss(), tanstackStart(), viteSolid({ ssr: true }),
  nitro()`; `resolve.tsconfigPaths`), `tsconfig.json` (strict, `@/*` → `src/*`,
  `noUncheckedIndexedAccess`), `vitest.config.ts` (unit, `src/**/*.test.ts`) and
  `vitest.integration.config.ts` (`src/**/*.integration.test.ts`, `setupFiles:
test/setup.ts`, serial). `test/setup.ts` throws when `DATABASE_URL` is unset.
- `src/routes/__root.tsx` (shell document, head meta, Tailwind stylesheet, root
  `notFoundComponent`), `src/routes/index.tsx` placeholder landing,
  `src/components/NotFound.tsx`, `src/router.tsx`, `src/styles/app.css`,
  `src/env.d.ts` (typed `VITE_*` vars), `src/routeTree.gen.ts` (generated by the
  router plugin; committed because CI typechecks before it builds).
- `src/server/logger.ts` — `log.info/warn/error(msg, meta?)`, one JSON line to
  stdout (error → stderr); `formatLogLine` unit-tested in `logger.test.ts`.
- `.env.example` with the five env vars and which Neon branch/Clerk instance each
  is for; `.gitignore` (`.env.*` except the example, `.output`, `*.sqlite3`);
  `README.md`; empty `data/`, `scripts/`, `public/` placeholders.
- Root `package.json`: `dev:website`, `build:website`, `lint:website`,
  `typecheck:website`, `test:website`; the three `*:website-svelte` scripts and
  the `@diddl/website-svelte` trustedDependencies entry removed.
- `.oxlintrc.json` / `.oxfmtrc.json`: ignore `apps/website/data/catalog.json`
  (replacing the svelte path), `apps/website/public/diddls/**`, and
  `**/routeTree.gen.ts`.
- `.github/workflows/ci.yaml`: website-only job on PRs and pushes to `main` —
  checkout → bun 1.3.10 → `bun install --frozen-lockfile` → `format:check` →
  `lint:website` → `typecheck:website` → unit tests → `build:website`. The
  `db:migrate` + integration-test steps are wired by "Server functions and the
  authorization-scoping suite" once the `TEST_DATABASE_URL*` secrets exist.

Checks run: `build:website` ✔ (Nitro output in `.output/`), `typecheck:website` ✔,
`lint:website` ✔, `test:website` ✔ (2 unit tests, integration "no test files"
exits 0), `format:check` ✔ (253 files), `dev:website` serves the SSR'd placeholder
on :3000 and returns 404 for unknown paths. CI on `main`: first run failed at
`bun install --frozen-lockfile` (the initial install left `bun.lock` in a
non-canonical postcss hoisting layout); fixed by re-running `bun install` and
committing the normalized lockfile — second run green:
https://github.com/Brandontam29/diddl-manager/actions/runs/32548906820.
Lesson for later tickets: run `bun install --frozen-lockfile` locally before
pushing dependency changes.

Facts later tickets depend on: import alias is `@/*`; unit tests live next to code
as `*.test.ts`, integration as `*.integration.test.ts`; env typing lives in
`src/env.d.ts`; `bun.lock` only gained entries (no existing package moved).
