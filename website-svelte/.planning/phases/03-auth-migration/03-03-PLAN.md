---
phase: 03-auth-migration
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/lists/listTypes.ts
  - src/lib/lists/listStore.svelte.ts
  - src/lib/lists/listStoreContext.svelte.ts
  - src/lib/lists/convexListStore.svelte.ts
  - src/lib/lists/migration.ts
autonomous: true
requirements: [AUTH-02, AUTH-03, AUTH-04]
must_haves:
  truths:
    - 'Guest mode keeps using the existing localStorage data model.'
    - 'List mutations can be awaited and return real entities where callers need them.'
    - 'Client migration logic clears guest storage only when the backend contract says it is safe.'
  artifacts:
    - path: 'src/lib/lists/listTypes.ts'
      provides: 'Shared async store contract'
      contains: 'Promise<GuestList>'
    - path: 'src/lib/lists/convexListStore.svelte.ts'
      provides: 'Authenticated store implementation'
      contains: 'createConvexListStore'
    - path: 'src/lib/lists/migration.ts'
      provides: 'Client migration orchestration'
      contains: 'safeToClearGuestData'
  key_links:
    - from: 'src/lib/lists/convexListStore.svelte.ts'
      to: 'src/convex/authed/lists.ts'
      via: 'Convex client calls'
      pattern: "api\\.authed\\.lists"
    - from: 'src/lib/lists/migration.ts'
      to: 'src/lib/lists/migrationResult.ts'
      via: 'status handling'
      pattern: 'fully_migrated|already_migrated|partial_or_retry_needed'
---

<objective>
Replace the Phase 2 synchronous guest-only store seam with a coherent dual-store contract.

Purpose: The current `ListStore` is synchronous and callers depend on `createList()` returning the created list. Phase 3 needs one honest contract for both guest and Convex stores, with no fake IDs, `_id` leaks, or `as any`.
Output: Updated list interfaces, async-capable guest store/context, Convex store implementation, and migration orchestration that obeys the explicit backend result contract.
</objective>

<execution_context>
@/home/btam/.codex/get-shit-done/workflows/execute-plan.md
@/home/btam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-guest-list-mode/02-02-SUMMARY.md
@.planning/phases/03-auth-migration/03-CONTEXT.md
@.planning/phases/03-auth-migration/03-01-PLAN.md
@src/lib/lists/listTypes.ts
@src/lib/lists/listStore.svelte.ts
@src/lib/lists/listStoreContext.svelte.ts

<interfaces>
Current `ListStore` from [listTypes.ts](/home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listTypes.ts):
```ts
createList(name: string, color: string): GuestList;
updateList(id: string, updates: Partial<Pick<GuestList, 'name' | 'color'>>): void;
deleteList(id: string): void;
addCatalogItems(listId: string, refs: CatalogReference[]): void;
removeItems(itemIds: string[]): void;
updateItems(...): void;
duplicateItems(itemIds: string[]): GuestListItem[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redefine the store contract around awaited mutations and stable UI shapes</name>
  <files>src/lib/lists/listTypes.ts</files>
  <action>Update `ListStore` so mutating methods are promise-based and `createList()` / `duplicateItems()` return real created entities. Keep `GuestList`, `GuestListItem`, and `CatalogReference` as the canonical UI and localStorage shapes so AUTH-03 remains true. This is the contract all caller rewiring in Plan 03-04 must follow.</action>
  <verify>
    <automated>rg -n "createList\\(name: string, color: string\\): Promise<GuestList>|duplicateItems\\(itemIds: string\\[\\]\\): Promise<GuestListItem\\[\\]>" src/lib/lists/listTypes.ts</automated>
  </verify>
  <done>The shared seam is honest about async behavior and still preserves the Phase 2 data model.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the guest store and context seam to the new contract</name>
  <files>src/lib/lists/listStore.svelte.ts, src/lib/lists/listStoreContext.svelte.ts</files>
  <action>Refactor the guest store to satisfy the awaited mutation contract without changing the stored payload shape. Expose stable helpers for reading guest storage, ensuring the guestSessionId, and clearing storage. Replace the one-time context value with a stable facade/delegate pattern so route components can survive guest/authed store swaps without stale references.</action>
  <verify>
    <automated>rg -n "Promise<GuestList>|guestSessionId|clearGuest|delegate|setStore|swap" src/lib/lists/listStore.svelte.ts src/lib/lists/listStoreContext.svelte.ts</automated>
  </verify>
  <done>Guest mode still works with the same persisted shape, and the app has a store container that can safely swap implementations.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ConvexListStore and client migration orchestration</name>
  <files>src/lib/lists/convexListStore.svelte.ts, src/lib/lists/migration.ts</files>
  <action>Create `ConvexListStore` against the Plan 03-01 authed functions and keep Convex IDs internal to that module. Implement client migration orchestration with retry/backoff for transient failures, but only clear localStorage when the backend returns `fully_migrated` or `already_migrated`. If the backend returns `partial_or_retry_needed`, preserve guest storage and surface a clear message instead of pretending migration succeeded.</action>
  <verify>
    <automated>bun run lint && bun run format && bun run check</automated>
  </verify>
  <done>The client can switch between guest and authenticated stores without fake IDs, and migration state is interpreted safely.</done>
</task>

</tasks>

<verification>
Run `bun run lint && bun run format && bun run check`.
</verification>

<success_criteria>

- `ListStore` has one coherent awaited mutation contract.
- Guest storage helpers and migration orchestration honor `safeToClearGuestData`.
- `ConvexListStore` consumes the authed API without leaking backend-only identifiers into the UI layer.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-auth-migration/03-03-SUMMARY.md`
</output>
