---
phase: 02-guest-list-mode
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/lists/listTypes.ts
  - src/lib/lists/listStore.svelte.ts
  - src/lib/lists/listStoreContext.svelte.ts
  - src/routes/app/+layout.svelte
autonomous: true
requirements:
  - LIST-04
  - LIST-05
  - LIST-06
  - LIST-07
  - LIST-08
  - LIST-09
  - LIST-10
  - LIST-11
  - LIST-12

must_haves:
  truths:
    - 'All Phase 2 pages code against a shared ListStore contract instead of localStorage directly per D-19.'
    - 'The guest implementation lives in a rune-backed `.svelte.ts` module, matching repo conventions for `$state`, `$derived`, and `$effect`.'
    - 'Guest persistence uses natural catalog keys (`type:number`) rather than Convex document IDs per D-20.'
    - 'The store enforces the one-list guest limit, auto-saves mutations, and surfaces storage failures through toast feedback per D-05, D-22, and D-23.'
    - 'The app layout provides one shared list store instance so overview, detail, and catalog flows stay in sync without an event bus per D-24.'
  artifacts:
    - path: 'src/lib/lists/listTypes.ts'
      provides: 'ListStore types, natural-key helpers, list/item factories, and condition metadata'
      exports:
        [
          'LIST_CONDITIONS',
          'ListStore',
          'GuestList',
          'GuestListItem',
          'CatalogReference',
          'catalogKeyForReference'
        ]
    - path: 'src/lib/lists/listStore.svelte.ts'
      provides: 'GuestListStore implementation using Svelte 5 runes and localStorage'
      exports: ['createGuestListStore']
    - path: 'src/lib/lists/listStoreContext.svelte.ts'
      provides: 'Context helpers for sharing the store across routes'
      exports: ['setListStoreContext', 'getListStoreContext']
    - path: 'src/routes/app/+layout.svelte'
      provides: 'Single-store app wiring and sonner host'
  key_links:
    - from: 'src/lib/lists/listStore.svelte.ts'
      to: 'localStorage'
      via: 'load/save helpers using $state.snapshot()'
      pattern: 'localStorage\\.(getItem|setItem)'
    - from: 'src/lib/lists/listStore.svelte.ts'
      to: 'src/lib/components/ui/sonner'
      via: 'toast-based storage error feedback'
      pattern: 'toast\\.(error|warning)'
    - from: 'src/lib/lists/listStoreContext.svelte.ts'
      to: 'src/routes/app/+layout.svelte'
      via: 'shared context provider'
      pattern: 'setListStoreContext'
---

<objective>
Build the Phase 2 handwritten foundation: contracts, guest rune store, shared context, and app-level wiring.

Purpose: Later list overview, list detail, and catalog select-mode plans should only compose UI around a stable shared store. This plan isolates the storage seam so pages do not invent their own persistence or selector logic.

Output:

- `src/lib/lists/listTypes.ts`
- `src/lib/lists/listStore.svelte.ts`
- `src/lib/lists/listStoreContext.svelte.ts`
- `src/routes/app/+layout.svelte` updated to provide the store and sonner host
  </objective>

<execution_context>
@/home/btam/.codex/get-shit-done/workflows/execute-plan.md
@/home/btam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-guest-list-mode/02-CONTEXT.md
@.planning/phases/02-guest-list-mode/02-RESEARCH.md
@.planning/phases/02-guest-list-mode/02-UI-SPEC.md
@src/lib/stores/clerk.svelte.ts
@src/routes/app/+layout.svelte

<interfaces>
Locked Phase 2 store decisions:
- D-05 guest limit = 1 list
- D-19 code against abstract ListStore
- D-20 natural catalog keys, not Convex IDs
- D-21 Svelte 5 runes for the guest store
- D-22 auto-save on every mutation
- D-23 toast on storage failure
- D-24 no custom event bus
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared list contracts and natural-key helpers</name>
  <files>src/lib/lists/listTypes.ts</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-CONTEXT.md
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-RESEARCH.md
    - /home/btam/other-code/diddl-manager/website-svelte/src/convex/schema.ts
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/stores/clerk.svelte.ts
  </read_first>
  <behavior>
    - Contract test 1: condition values are exactly `mint`, `near_mint`, `good`, `poor`, `damaged`.
    - Contract test 2: a helper round-trips `{ type, number }` to a stable `type:number` key and back.
    - Contract test 3: the ListStore interface exposes the selectors and mutations that overview, detail, and catalog pages will call directly.
  </behavior>
  <action>
Create `src/lib/lists/listTypes.ts` with:
- `CatalogReference`, `GuestList`, `GuestListItem`, summary/count/filter types
- `LIST_CONDITIONS` metadata for UI labels
- `ListStore` interface with list CRUD, item CRUD, bulk update, duplicate, count, completion, and owned-key helpers
- pure helpers for `catalogKeyForReference`, `catalogReferenceFromKey`, and default guest list/item creation

Keep this file free of UI and localStorage code. It is the source of truth for all later Phase 2 imports.
</action>
<verify>
<automated>test -f /home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listTypes.ts && rg -n "LIST_CONDITIONS|interface ListStore|catalogKeyForReference|catalogReferenceFromKey|near_mint" /home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listTypes.ts</automated>
</verify>
<acceptance_criteria> - `src/lib/lists/listTypes.ts` exports `ListStore` - Natural-key helpers are defined for `type:number` - The file does not reference Convex IDs or route-specific UI state
</acceptance_criteria>
<done>The phase has a single contract file for guest-list types, helpers, and store surface area.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the rune-backed GuestListStore and share it through app layout</name>
  <files>src/lib/lists/listStore.svelte.ts, src/lib/lists/listStoreContext.svelte.ts, src/routes/app/+layout.svelte</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listTypes.ts
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/stores/clerk.svelte.ts
    - /home/btam/other-code/diddl-manager/website-svelte/src/routes/app/+layout.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-CONTEXT.md
  </read_first>
  <behavior>
    - Store test 1: `createList()` rejects a second guest list even if called directly.
    - Store test 2: mutations snapshot state and save a single JSON payload to localStorage.
    - Store test 3: duplicate entries remain independent so the same catalog reference can be tracked multiple times.
  </behavior>
  <action>
Create `src/lib/lists/listStore.svelte.ts` as the concrete guest implementation of `ListStore`.

Implementation requirements:

- Use `$state`, `$derived`, and `$effect` in `.svelte.ts` form, matching repo conventions.
- Persist a single guest payload under one storage key and serialize with `$state.snapshot()`.
- Hydrate safely in the browser, tolerating empty or malformed storage.
- Enforce guest one-list limit in `createList()`.
- Implement selectors for completion percentage, item counts, missing-only subsets, owned keys, and owned counts by type/range.
- Implement `addCatalogItems`, `removeItems`, `updateItems`, and `duplicateItems`.

Create `src/lib/lists/listStoreContext.svelte.ts` to expose `setListStoreContext()` and `getListStoreContext()`.

Update `src/routes/app/+layout.svelte` to instantiate the guest store once, provide it through context, and mount the sonner host for storage warnings/errors.

Finish with:

```bash
bun run lint
bun run format
bun run check
```

  </action>
  <verify>
    <automated>rg -n "localStorage\\.(getItem|setItem)|\\$state\\.snapshot|createList\\(|addCatalogItems|duplicateItems|getCompletionPercent|getOwnedCatalogKeys|toast\\.(error|warning)" /home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listStore.svelte.ts && rg -n "setListStoreContext|getListStoreContext" /home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listStoreContext.svelte.ts && rg -n "setListStoreContext|Toaster|Sonner" /home/btam/other-code/diddl-manager/website-svelte/src/routes/app/+layout.svelte && cd /home/btam/other-code/diddl-manager/website-svelte && bun run lint && bun run format && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lists/listStore.svelte.ts` uses Svelte 5 runes and localStorage
    - The store path uses `.svelte.ts`, not plain `.ts`
    - Guest-limit enforcement, auto-save, and storage toast behavior are implemented in the store
    - `src/routes/app/+layout.svelte` provides one shared list store instance and the sonner host
    - `bun run lint`, `bun run format`, and `bun run check` all succeed
  </acceptance_criteria>
  <done>The guest list store exists as a repo-conformant rune module and is shared through the app shell.</done>
</task>

</tasks>

<verification>
The Phase 2 foundation is valid when list pages can import one shared ListStore contract and store instance without owning their own storage or global synchronization logic.
</verification>

<success_criteria>
Guest list persistence, selectors, and shared app wiring are solved once so later plans can focus on UI behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-guest-list-mode/02-02-SUMMARY.md`
</output>
