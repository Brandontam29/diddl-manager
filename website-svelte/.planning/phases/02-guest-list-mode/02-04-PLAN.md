---
phase: 02-guest-list-mode
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/lib/components/lists/ListItemCard.svelte
  - src/lib/components/lists/ListSidebar.svelte
  - src/lib/components/lists/ListToolbar.svelte
  - src/lib/components/lists/QuantityStepper.svelte
  - src/routes/app/lists/[id]/+page.svelte
autonomous: true
requirements:
  - LIST-05
  - LIST-06
  - LIST-07
  - LIST-08
  - LIST-09
  - LIST-10
  - LIST-11
  - LIST-12

must_haves:
  truths:
    - 'Opening `/app/lists/[id]` shows owned items with quantity, selection, and completion state.'
    - 'Bulk toolbar actions cover duplicate, complete, incomplete, condition change, and remove.'
    - 'Missing-only and show-unowned can be used together.'
    - 'Unowned cards have an explicit add action wired to `addCatalogItems(listId, [reference])`, satisfying LIST-10 directly from the detail page.'
    - 'Sidebar counts combine catalog totals with guest-owned counts.'
  artifacts:
    - path: 'src/lib/components/lists/ListItemCard.svelte'
      provides: 'Owned/unowned item rendering with explicit add callback for unowned cards'
    - path: 'src/lib/components/lists/ListToolbar.svelte'
      provides: 'Default and selected-item toolbar states'
    - path: 'src/lib/components/lists/ListSidebar.svelte'
      provides: 'Category tree with owned/total count toggle'
    - path: 'src/routes/app/lists/[id]/+page.svelte'
      provides: 'List detail page wiring'
  key_links:
    - from: 'src/routes/app/lists/[id]/+page.svelte'
      to: 'api.authed.catalog.listByRange'
      via: 'show-unowned range query'
      pattern: 'listByRange'
    - from: 'src/routes/app/lists/[id]/+page.svelte'
      to: 'src/lib/lists/listStore.svelte.ts'
      via: 'add/remove/update/duplicate item operations'
      pattern: 'addCatalogItems|removeItems|updateItems|duplicateItems'
---

<objective>
Deliver the list detail page where guests manage tracked items and add missing ones in context.

Purpose: This is the core behavior of Guest List Mode. The detail page must feel complete before auth exists, including duplication, conditions, quantity control, completion, filtering, and inline add-from-detail.

Output:

- Detail-page item card, sidebar, toolbar, and quantity stepper components
- `/app/lists/[id]` page with owned-item management and show-unowned add flow
  </objective>

<execution_context>
@/home/btam/.codex/get-shit-done/workflows/execute-plan.md
@/home/btam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-guest-list-mode/02-CONTEXT.md
@.planning/phases/02-guest-list-mode/02-RESEARCH.md
@.planning/phases/02-guest-list-mode/02-UI-SPEC.md
@src/lib/lists/listTypes.ts
@src/lib/lists/listStore.svelte.ts
@src/lib/lists/listStoreContext.svelte.ts
@src/lib/components/catalog/CatalogSidebar.svelte
@src/lib/components/catalog/LazyImage.svelte
@src/convex/authed/catalog.ts
@src/convex/authed/diddlTypes.ts

<interfaces>
Locked detail decisions:
- D-08 show-unowned
- D-09 shared category-tree mental model
- D-11 grid cards with quantity stepper
- D-12 checkbox selection
- D-13 taskbar bulk actions
- D-14 condition via taskbar
- D-16 missing-only filter in toolbar
- D-17 completion at detail header
- D-18 owned/total count toggle in sidebar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build prop-driven detail-page components</name>
  <files>src/lib/components/lists/QuantityStepper.svelte, src/lib/components/lists/ListItemCard.svelte, src/lib/components/lists/ListToolbar.svelte, src/lib/components/lists/ListSidebar.svelte</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-UI-SPEC.md
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-CONTEXT.md
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/catalog/CatalogSidebar.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/catalog/LazyImage.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/dropdown-menu/index.ts
  </read_first>
  <action>
Create:
- `QuantityStepper.svelte` with increment/decrement callbacks and accessible labels
- `ListItemCard.svelte` for both owned and unowned states; unowned state must expose an explicit callback like `onAddUnowned(reference)`
- `ListToolbar.svelte` for default toolbar state and selected-item bulk action state
- `ListSidebar.svelte` reusing the catalog accordion mental model with owned/total counts

Keep these components prop-driven. They should not fetch data or own store instances.
</action>
<verify>
<automated>rg -n "Decrease quantity|Increase quantity" /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/QuantityStepper.svelte && rg -n "Duplicate|Mark Complete|Mark Incomplete|Condition|Remove|Add to List|onAddUnowned" /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/ListItemCard.svelte /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/ListToolbar.svelte && rg -n "Accordion|owned/total|count" /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/ListSidebar.svelte</automated>
</verify>
<acceptance_criteria> - `ListItemCard.svelte` supports owned and unowned states - Unowned cards expose a concrete add callback - `ListToolbar.svelte` contains the required bulk actions - `ListSidebar.svelte` exposes owned/total count display
</acceptance_criteria>
<done>The detail-page component layer exists and exposes the hooks the route needs for full item management.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `/app/lists/[id]` for bulk editing, filtering, and inline add-from-detail</name>
  <files>src/routes/app/lists/[id]/+page.svelte</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/src/routes/app/catalog/+page.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listStoreContext.svelte.ts
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/ListToolbar.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/ListSidebar.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-RESEARCH.md
  </read_first>
  <action>
Create `src/routes/app/lists/[id]/+page.svelte`:
- load the target list or redirect back to `/app/lists`
- query `api.authed.diddlTypes.list`, `api.authed.catalog.countByType`, and `api.authed.catalog.listByRange`
- derive owned items, missing-only subset, and optional unowned append set
- keep selection, missing-only, show-unowned, and pending condition state in the page
- wire bulk toolbar actions to `removeItems`, `duplicateItems`, and `updateItems`
- wire the unowned-card add action to `addCatalogItems(listId, [reference])`
- compute sidebar owned/total counts from catalog totals plus store-owned counts

Finish with:

```bash
bun run lint
bun run format
bun run check
```

  </action>
  <verify>
    <automated>rg -n "showUnowned|missingOnly|selectedItemIds|listByRange|getOwnedCatalogKeys|addCatalogItems|duplicateItems|removeItems|updateItems|CompletionBadge" /home/btam/other-code/diddl-manager/website-svelte/src/routes/app/lists/[id]/+page.svelte && cd /home/btam/other-code/diddl-manager/website-svelte && bun run lint && bun run format && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `/app/lists/[id]` supports quantity, completion, condition, duplicate, and remove flows
    - Missing-only and show-unowned can be enabled together
    - Unowned items can be added through `addCatalogItems()` without leaving the page
    - Completion and counts update immediately after mutations
    - `bun run lint`, `bun run format`, and `bun run check` all succeed
  </acceptance_criteria>
  <done>The detail page supports the full guest item-management loop, including inline add-from-detail.</done>
</task>

</tasks>

<verification>
On `/app/lists/[id]`, a guest can duplicate, edit, complete, remove, filter, and add items from the current range without leaving the detail page or losing local state.
</verification>

<success_criteria>
LIST-05 through LIST-12 are satisfied on the list detail page, including the explicit add-from-detail path required by LIST-10.
</success_criteria>

<output>
After completion, create `.planning/phases/02-guest-list-mode/02-04-SUMMARY.md`
</output>
