---
phase: 03-auth-migration
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - .planning/phases/03-auth-migration/03-VALIDATION.md
  - .planning/phases/03-auth-migration/03-05-SUMMARY.md
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-04, AUTH-05]
must_haves:
  truths:
    - 'Guest mode still works after auth is added.'
    - 'A successful migration clears guest storage only after all guest data is preserved.'
    - 'Blocked or failed migrations leave guest storage intact and explain what happened.'
  artifacts:
    - path: '.planning/phases/03-auth-migration/03-VALIDATION.md'
      provides: 'Phase 3 verification map'
      contains: 'partial_or_retry_needed'
  key_links:
    - from: '.planning/phases/03-auth-migration/03-VALIDATION.md'
      to: 'src/routes/app/+layout.svelte'
      via: 'manual flow checks'
      pattern: 'migration'
---

<objective>
Run the Phase 3 validation matrix and pause for end-to-end human verification.

Purpose: Auth + migration is the highest-risk user-data path in the milestone. It needs explicit validation against the migration status contract, route wiring, and guest fallback behavior.
Output: Automated verification run plus a human checkpoint using `03-VALIDATION.md`.
</objective>

<execution_context>
@/home/btam/.codex/get-shit-done/workflows/execute-plan.md
@/home/btam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-auth-migration/03-VALIDATION.md
@.planning/phases/03-auth-migration/03-CONTEXT.md
@src/routes/app/+layout.svelte
@src/lib/lists/migration.ts
@src/convex/authed/migration.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the automated Phase 3 validation commands</name>
  <files>.planning/phases/03-auth-migration/03-VALIDATION.md, .planning/phases/03-auth-migration/03-05-SUMMARY.md</files>
  <action>Execute the automated command sequence documented in `03-VALIDATION.md`. If any Convex file changed during execution, include `bun run convex:gen` first; always finish with `bun run lint`, `bun run format`, and `bun run check`. Record the automated run result in `03-VALIDATION.md` and/or the final summary so the validation artifact reflects what was actually executed.</action>
  <verify>
    <automated>bun run lint && bun run format && bun run check</automated>
  </verify>
  <done>The repo-level automated validation passes for the final Phase 3 state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify guest, success, idempotent, and blocked migration paths</name>
  <files></files>
  <action>Use the manual scenarios in `03-VALIDATION.md` to confirm the migration result contract is visible in real user behavior, especially that blocked outcomes preserve guest storage.</action>
  <what-built>Full Phase 3 auth shell, dual-store switching, explicit migration statuses, and guest fallback behavior.</what-built>
  <how-to-verify>
1. Start from guest mode, create a list, add items, and refresh to confirm guest persistence still works.
2. Confirm the Clerk modal offers both Email+Password and Google OAuth, and verify at least one sign-up path succeeds.
3. Sign in with an account under the authed list cap and confirm migration completes, lists appear in the authed view, and guest storage is cleared only after the success path.
4. Refresh while signed in and confirm the session persists.
5. Sign out and confirm redirect to `/`.
6. Sign back in with the same migrated guestSessionId and confirm the app behaves idempotently without duplicates.
7. Test a blocked migration case from `03-VALIDATION.md` such as a list-cap conflict, and confirm the UI reports a non-success outcome while leaving guest storage intact.
  </how-to-verify>
  <verify>
    <automated>bun run lint && bun run format && bun run check</automated>
  </verify>
  <done>The human verifier confirms all required Phase 3 scenarios in `03-VALIDATION.md`, or returns the exact failing scenario for follow-up work.</done>
  <resume-signal>Type "approved" or list the failing validation scenario(s).</resume-signal>
</task>

</tasks>

<verification>
Follow `.planning/phases/03-auth-migration/03-VALIDATION.md`.
</verification>

<success_criteria>

- Automated validation passes.
- Human verification confirms provider availability, sign-up/sign-in behavior, guest fallback, successful migration, idempotent replay, and blocked-migration preservation behavior.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-auth-migration/03-05-SUMMARY.md`
</output>
