---
phase: 03-auth-migration
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - src/routes/app/+layout.svelte
  - src/routes/app/+page.svelte
  - src/routes/app/lists/+page.svelte
  - src/routes/app/catalog/+page.svelte
  - src/routes/app/lists/[id]/+page.svelte
  - src/lib/components/catalog/SidebarTypeRow.svelte
  - src/lib/components/catalog/SidebarRangeRow.svelte
  - src/lib/components/lists/CreateListDialog.svelte
  - src/lib/components/lists/EditListDialog.svelte
  - src/lib/components/lists/DeleteListDialog.svelte
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-04, AUTH-05]
must_haves:
  truths:
    - 'Signing in on an `/app` route swaps the store in place and shows migration loading feedback.'
    - 'All list flows await the real store contract and keep working for both guests and authenticated users.'
    - 'Both D-13 upgrade prompts appear in the correct guest-only locations.'
    - 'D-16 is enforced: sidebar completion percentages are only visible when authenticated.'
  artifacts:
    - path: 'src/routes/app/+layout.svelte'
      provides: 'Store-switching app shell'
      contains: 'ShimmerGrid'
    - path: 'src/lib/components/lists/CreateListDialog.svelte'
      provides: 'Awaited create-list flow with second-list friction prompt'
      contains: 'createList'
    - path: 'src/routes/app/lists/+page.svelte'
      provides: 'Lists overview with footer upgrade prompt'
      contains: 'sync across devices'
    - path: 'src/lib/components/catalog/SidebarTypeRow.svelte'
      provides: 'Auth-gated type completion display'
      contains: 'completion'
  key_links:
    - from: 'src/routes/app/+layout.svelte'
      to: 'src/lib/lists/migration.ts'
      via: 'sign-in migration orchestration'
      pattern: 'migrateGuest'
    - from: 'src/routes/app/catalog/+page.svelte'
      to: 'src/lib/components/lists/CreateListDialog.svelte'
      via: 'awaited list creation before add'
      pattern: 'onCreated'
    - from: 'src/routes/app/catalog/+page.svelte'
      to: 'src/lib/components/catalog/SidebarTypeRow.svelte'
      via: 'guest passes null completion values, authed passes real completion values'
      pattern: 'completion|completionPercent|null'
---

<objective>
Rewire the existing Phase 2 routes and dialogs to the new store contract and auth shell.

Purpose: The app already has working guest flows. This plan makes those flows work unchanged for guests while adding in-place auth switching, real awaited mutations, and the exact D-13 upgrade prompt placements.
Output: Store-switching app layout plus updated route/dialog callers that consume the new contract cleanly.
</objective>

<execution_context>
@/home/btam/.codex/get-shit-done/workflows/execute-plan.md
@/home/btam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-guest-list-mode/02-03-SUMMARY.md
@.planning/phases/02-guest-list-mode/02-04-SUMMARY.md
@.planning/phases/02-guest-list-mode/02-05-SUMMARY.md
@.planning/phases/03-auth-migration/03-CONTEXT.md
@.planning/phases/03-auth-migration/03-03-PLAN.md
@src/routes/app/+layout.svelte
@src/routes/app/catalog/+page.svelte
@src/routes/app/lists/+page.svelte
@src/routes/app/lists/[id]/+page.svelte
@src/lib/components/catalog/SidebarTypeRow.svelte
@src/lib/components/catalog/SidebarRangeRow.svelte
@src/lib/components/lists/CreateListDialog.svelte

<interfaces>
Call sites currently depending on synchronous mutations:
```ts
const newList = store.createList(name.trim(), color);
store.addCatalogItems(listId, refs);
const newItems = store.duplicateItems(Array.from(selectedItemIds));
store.updateItems([...], { quantity: v });
store.deleteList(list.id);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the `/app` shell for in-place auth switching and migration feedback</name>
  <files>src/routes/app/+layout.svelte, src/routes/app/+page.svelte</files>
  <action>Replace the Phase 2 always-guest layout with a store-switching shell that uses the stable context facade from Plan 03-03 plus the `AppHeader` and `ShimmerGrid` from Plan 03-02. Per D-06, sign-in on an `/app` route must swap to the authenticated store without a full reload. Per D-09 through D-11, the layout must show migration loading feedback, retry transient failures, and keep guest mode intact if migration cannot fully complete.</action>
  <verify>
    <automated>rg -n "AppHeader|ShimmerGrid|createConvexListStore|createGuestListStore|migrateGuest" src/routes/app/+layout.svelte src/routes/app/+page.svelte</automated>
  </verify>
  <done>The app shell can transition between guest and authed state in place, and `/app/+page.svelte` no longer owns a duplicate header.</done>
</task>

<task type="auto">
  <name>Task 2: Update dialogs and the lists overview for awaited mutations and both D-13 prompts</name>
  <files>src/lib/components/lists/CreateListDialog.svelte, src/lib/components/lists/EditListDialog.svelte, src/lib/components/lists/DeleteListDialog.svelte, src/routes/app/lists/+page.svelte</files>
  <action>Update all list dialogs to await the new `ListStore` methods and disable/re-enable UI while requests run. In `CreateListDialog` and the lists overview, implement the exact D-13 placements: the second-list friction prompt for guests when they hit the guest cap, and the guest-only footer prompt on the lists page. Keep D-15 explicit: guests stay capped at one list, authenticated users at three lists.</action>
  <verify>
    <automated>rg -n "await store\\.(createList|updateList|deleteList)|Sign up to create more lists|Sign up to sync across devices" src/lib/components/lists/CreateListDialog.svelte src/lib/components/lists/EditListDialog.svelte src/lib/components/lists/DeleteListDialog.svelte src/routes/app/lists/+page.svelte</automated>
  </verify>
  <done>The overview and CRUD dialogs use the real async contract, and both guest upsell prompts are present in the required locations.</done>
</task>

<task type="auto">
  <name>Task 3: Update catalog and list-detail callers to await real results and gate sidebar completion for auth only</name>
  <files>src/routes/app/catalog/+page.svelte, src/routes/app/lists/[id]/+page.svelte, src/lib/components/catalog/SidebarTypeRow.svelte, src/lib/components/catalog/SidebarRangeRow.svelte</files>
  <action>Refactor the catalog bulk-add and list-detail bulk-edit flows to await the new store methods, consume the actual list returned from `createList()`, and handle errors without hidden hacks. Remove any dead sync assumptions like unused duplicate return values or implicit immediate persistence. Implement D-16 explicitly in the sidebar completion path: guests must receive no completion percentage props/rendering, while authenticated users receive the real completion values derived from the authenticated store state. Keep guest and authenticated behavior aligned on the same route code.</action>
  <verify>
    <automated>rg -n "completion|completionPercent|null" src/routes/app/catalog/+page.svelte src/lib/components/catalog/SidebarTypeRow.svelte src/lib/components/catalog/SidebarRangeRow.svelte && bun run lint && bun run format && bun run check</automated>
  </verify>
  <done>The remaining Phase 2 routes compile and behave against the new store contract for both guest and authed users.</done>
</task>

</tasks>

<verification>
Run `bun run lint && bun run format && bun run check`.
</verification>

<success_criteria>

- The `/app` shell swaps stores in place and surfaces migration status honestly.
- Every real `ListStore` caller has been updated to await the new contract.
- The second-list and footer upgrade prompts are both wired exactly once in guest mode.
- Sidebar completion percentages render only for authenticated users.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-auth-migration/03-04-SUMMARY.md`
</output>
