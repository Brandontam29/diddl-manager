---
phase: 03-auth-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/lists/migrationResult.ts
  - src/convex/schema.ts
  - src/convex/authed/lists.ts
  - src/convex/authed/listItems.ts
  - src/convex/authed/migration.ts
  - src/convex/private/migration.ts
autonomous: true
requirements: [AUTH-03, AUTH-04]
must_haves:
  truths:
    - 'Guest migration returns an explicit status instead of treating partial preservation as success.'
    - 'Authenticated list CRUD uses the same list and list-item shapes the client store expects.'
    - 'Guest data remains in localStorage whenever migration cannot fully preserve it.'
    - "Backend-only migration coordination is planned through the repo's `private` Convex pattern rather than only client-facing authed functions."
  artifacts:
    - path: 'src/lib/lists/migrationResult.ts'
      provides: 'Shared migration result contract'
      contains: 'fully_migrated'
    - path: 'src/convex/schema.ts'
      provides: 'Server-side migration tracking table'
      contains: 'guestSessionId'
    - path: 'src/convex/authed/migration.ts'
      provides: 'Authed migration entrypoint with preflight + idempotency'
      exports: ['migrateGuestData']
    - path: 'src/convex/private/migration.ts'
      provides: 'SvelteKit-backend-only bridge endpoints for migration-related server orchestration where needed outside the Convex runtime'
      exports: ['preflightGuestMigration', 'runGuestMigration']
  key_links:
    - from: 'src/convex/authed/migration.ts'
      to: 'src/lib/lists/migrationResult.ts'
      via: 'shared discriminant names'
      pattern: 'fully_migrated|already_migrated|partial_or_retry_needed'
    - from: 'src/convex/private/migration.ts'
      to: 'src/lib/services/convex.ts'
      via: 'SvelteKit backend calls private endpoints through ConvexPrivateService'
      pattern: 'ConvexPrivateService'
    - from: 'src/convex/authed/migration.ts'
      to: 'src/convex/schema.ts'
      via: 'guest migration record lookup'
      pattern: 'guestSessionId'
---

<objective>
Define the server contract for authenticated list storage and lossless guest migration.

Purpose: Phase 3 cannot be wired safely until the backend can distinguish a complete migration from a blocked one and can return real list data without fake IDs or hidden side channels.
Output: Shared migration result types, migration tracking schema, authed list/list-item functions, and a migration action that never reports success while dropping guest data.
</objective>

<execution_context>
@/home/btam/.codex/get-shit-done/workflows/execute-plan.md
@/home/btam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-auth-migration/03-CONTEXT.md
@.planning/phases/03-auth-migration/03-RESEARCH.md
@src/convex/schema.ts
@src/convex/authed/helpers.ts
@src/convex/private/helpers.ts
@src/lib/services/convex.ts
@src/lib/lists/listTypes.ts

<interfaces>
From [listTypes.ts](/home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listTypes.ts):
```ts
export interface GuestList {
	id: string;
	name: string;
	color: string;
	createdAt: number;
}

export interface GuestListItem {
id: string;
listId: string;
catalogRef: CatalogReference;
condition: ItemCondition;
quantity: number;
complete: boolean;
tags: string[];
}

````

From [helpers.ts](/home/btam/other-code/diddl-manager/website-svelte/src/convex/authed/helpers.ts):
```ts
export const authedQuery = customQuery(query, { ... });
export const authedMutation = customMutation(mutation, { ... });
export const authedAction = customAction(action, { ... });
````

From [private/helpers.ts](/home/btam/other-code/diddl-manager/website-svelte/src/convex/private/helpers.ts):

```ts
export const privateQuery = customQuery(query, apiKeyGuard);
export const privateMutation = customMutation(mutation, apiKeyGuard);
export const privateAction = customAction(action, apiKeyGuard);
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the migration result contract and tracking schema</name>
  <files>src/lib/lists/migrationResult.ts, src/convex/schema.ts</files>
  <action>Create `src/lib/lists/migrationResult.ts` with a shared discriminated union covering `fully_migrated`, `already_migrated`, and `partial_or_retry_needed`, including `safeToClearGuestData` and machine-readable reasons. In `src/convex/schema.ts`, add a migration tracking table keyed by `ownerSubject + guestSessionId` so replay detection is authoritative per CF-03. Per D-08 and D-15, document in the contract that list-cap conflicts are a partial outcome, not success.</action>
  <verify>
    <automated>test -f src/lib/lists/migrationResult.ts && rg -n "fully_migrated|already_migrated|partial_or_retry_needed|safeToClearGuestData" src/lib/lists/migrationResult.ts src/convex/schema.ts</automated>
  </verify>
  <done>The repo has one shared migration status contract, and the schema can record completed guestSessionId migrations without ambiguity.</done>
</task>

<task type="auto">
  <name>Task 2: Implement authed list and list-item functions that return real UI shapes</name>
  <files>src/convex/authed/lists.ts, src/convex/authed/listItems.ts</files>
  <action>Build client-facing authed queries and mutations using the `src/convex/authed` wrappers only. Return mapped list/list-item objects shaped like `GuestList` and `GuestListItem` instead of raw Convex docs, so the client store does not need fake IDs, `_id` side channels, or `as any`. Enforce ownership on every mutation and keep the authed three-list cap from D-15 in the create path. Keep backend-only multi-step coordination out of these handlers so migration orchestration can follow the repo's private/backend conventions.</action>
  <verify>
    <automated>rg -n "export const .*create|export const .*update|export const .*remove|ownerSubject|by_owner|by_list" src/convex/authed/lists.ts src/convex/authed/listItems.ts</automated>
  </verify>
  <done>The authenticated API surface exists and returns the real list entities the client contract can await and render directly.</done>
</task>

<task type="auto">
  <name>Task 3: Implement preflight-first migration logic with explicit D-08/D-15 conflict handling</name>
  <files>src/convex/authed/migration.ts, src/convex/private/migration.ts</files>
  <action>Implement the migration flow with the repo's actual boundaries. `src/convex/authed/migration.ts` is the browser-facing entrypoint exposed to authenticated clients. Inside the Convex runtime, multi-step orchestration must use shared helpers and/or internal mutations rather than pretending `private` endpoints are callable from authed Convex code. `src/convex/private/migration.ts` is reserved for SvelteKit-backend entrypoints that need migration-related server orchestration through `ConvexPrivateService` and Effect v4. Resolve the D-08 vs D-15 conflict up front: name collisions merge into an existing authed list with item dedupe; any remaining guest lists that would exceed the authed three-list cap must return `partial_or_retry_needed` with `safeToClearGuestData: false` and perform no writes. Likewise, unresolved `type:number` references must produce `partial_or_retry_needed` rather than a false success. Only write records and store a completed migration row when the outcome is `fully_migrated`; return `already_migrated` when the guestSessionId has already been completed.</action>
  <verify>
    <automated>bun run convex:gen && bun run lint && bun run format && bun run check</automated>
  </verify>
  <done>Migration is idempotent, lossless-by-contract, and incapable of clearing guest data after a partial preservation outcome.</done>
</task>

</tasks>

<verification>
Run `bun run convex:gen && bun run lint && bun run format && bun run check`.
</verification>

<success_criteria>

- Migration returns `fully_migrated`, `already_migrated`, or `partial_or_retry_needed` with no ambiguous success state.
- D-08 merge behavior is bounded by D-15 list-cap preflight instead of silently dropping lists.
- The backend returns real list and item shapes consumable by the client store without hacks.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-auth-migration/03-01-SUMMARY.md`
</output>
