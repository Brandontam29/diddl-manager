---
phase: 02-guest-list-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - src/lib/components/ui/alert-dialog
  - src/lib/components/ui/button
  - src/lib/components/ui/checkbox
  - src/lib/components/ui/dialog
  - src/lib/components/ui/dropdown-menu
  - src/lib/components/ui/input
  - src/lib/components/ui/label
  - src/lib/components/ui/separator
  - src/lib/components/ui/sonner
  - src/lib/components/ui/toggle
autonomous: true
requirements: []

must_haves:
  truths:
    - 'Phase 2 shadcn primitives exist before any list UI is implemented.'
    - 'Dialog, destructive confirmation, checkbox, toolbar, form, and toast primitives are generated from the project-standard shadcn-svelte tooling.'
    - 'Generated component import paths match the existing `$lib/components/ui/*` convention used elsewhere in the repo.'
  artifacts:
    - path: 'src/lib/components/ui/dialog'
      provides: 'Modal primitives for create/edit list flows'
    - path: 'src/lib/components/ui/alert-dialog'
      provides: 'Confirmation dialog primitives for destructive delete flows'
    - path: 'src/lib/components/ui/sonner'
      provides: 'Toast host primitives for guest-storage failures'
  key_links:
    - from: 'package.json'
      to: 'src/lib/components/ui/dialog'
      via: 'bun x shadcn-svelte@latest add ...'
      pattern: 'shadcn-svelte'
---

<objective>
Install the UI primitives Phase 2 depends on before writing any guest-list store or page code.

Purpose: This isolates the generated shadcn work into a small, low-risk setup plan so the foundation and page plans do not mix package churn with handwritten store/UI logic.

Output:

- Phase 2 shadcn primitives generated under `src/lib/components/ui/`
- Package metadata updated only as required by the generator
  </objective>

<execution_context>
@/home/btam/.codex/get-shit-done/workflows/execute-plan.md
@/home/btam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-guest-list-mode/02-RESEARCH.md
@.planning/phases/02-guest-list-mode/02-UI-SPEC.md
@package.json
@src/lib/components/ui/card/index.ts

<interfaces>
Phase 2 requires these shadcn component families from research and UI-SPEC:
- dialog
- alert-dialog
- button
- checkbox
- dropdown-menu
- input
- label
- separator
- toggle
- sonner
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the required shadcn primitives for Guest List Mode</name>
  <files>package.json, bun.lock, src/lib/components/ui/alert-dialog, src/lib/components/ui/button, src/lib/components/ui/checkbox, src/lib/components/ui/dialog, src/lib/components/ui/dropdown-menu, src/lib/components/ui/input, src/lib/components/ui/label, src/lib/components/ui/separator, src/lib/components/ui/sonner, src/lib/components/ui/toggle</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/package.json
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-RESEARCH.md
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-UI-SPEC.md
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/card/index.ts
  </read_first>
  <action>
Run the Phase 2 shadcn generator command exactly once:

```bash
bun x shadcn-svelte@latest add dialog alert-dialog button checkbox dropdown-menu input label separator toggle sonner
```

Do not hand-edit generated files during this plan unless the generator introduces a repo-local compile or lint issue that blocks later work. The goal here is to establish the component directories and export surfaces that later plans will import.
</action>
<verify>
<automated>test -d /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/dialog && test -d /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/alert-dialog && test -d /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/checkbox && test -d /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/toggle && test -d /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/sonner</automated>
</verify>
<acceptance_criteria> - All ten required `src/lib/components/ui/*` directories exist - The generated files follow the repo's existing shadcn export structure - No guest-list store or route code is added in this plan
</acceptance_criteria>
<done>Phase 2 UI primitives are installed and ready for import by the foundation and page plans.</done>
</task>

</tasks>

<verification>
The plan is complete when the required shadcn directories exist and later plans can import dialog, alert-dialog, checkbox, dropdown-menu, toggle, and sonner primitives without additional generator work.
</verification>

<success_criteria>
Phase 2 has the component primitives it needs, separated from handwritten store and route implementation work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-guest-list-mode/02-01-SUMMARY.md`
</output>
