---
phase: 03-auth-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/stores/clerk.svelte.ts
  - src/routes/+page.svelte
  - src/lib/components/landing/LandingHeader.svelte
  - src/lib/components/landing/FeatureCard.svelte
  - src/lib/components/layout/AppHeader.svelte
  - src/lib/components/layout/ShimmerGrid.svelte
  - src/lib/components/layout/UpgradePrompt.svelte
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-05]
must_haves:
  truths:
    - 'Guests can land on `/`, understand the product, and choose guest mode or Clerk sign-in.'
    - 'Authenticated users see the Clerk user button, and sign-out sends them back to `/`.'
    - 'The app shell has reusable auth UI for migration shimmer and upgrade prompts.'
    - 'Phase execution explicitly verifies the Clerk dashboard has both Email+Password and Google OAuth enabled per D-02.'
  artifacts:
    - path: 'src/routes/+page.svelte'
      provides: 'Landing page'
      contains: 'Try as Guest'
    - path: 'src/lib/components/layout/AppHeader.svelte'
      provides: 'Shared app header with auth-aware actions'
      contains: 'openSignIn'
    - path: 'src/lib/stores/clerk.svelte.ts'
      provides: 'Clerk redirect + theme configuration'
      contains: "afterSignOutUrl: '/'"
    - path: '.planning/phases/03-auth-migration/03-02-SUMMARY.md'
      provides: 'Recorded Clerk provider configuration/verification note'
      contains: 'Email + password'
  key_links:
    - from: 'src/lib/components/layout/AppHeader.svelte'
      to: 'src/lib/stores/clerk.svelte.ts'
      via: 'getClerkContext'
      pattern: 'getClerkContext'
    - from: 'src/routes/+page.svelte'
      to: 'src/lib/components/landing/LandingHeader.svelte'
      via: 'landing composition'
      pattern: 'LandingHeader'
---

<objective>
Build the auth shell and public landing experience for Phase 3.

Purpose: D-01 through D-05 and D-12 through D-14 need a stable UI layer before store switching and migration wiring land.
Output: Clerk theme/redirect updates, landing page, auth-aware app header, and reusable shimmer and upgrade prompt components.
</objective>

<execution_context>
@/home/btam/.codex/get-shit-done/workflows/execute-plan.md
@/home/btam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-auth-migration/03-CONTEXT.md
@.planning/phases/03-auth-migration/03-UI-SPEC.md
@src/lib/stores/clerk.svelte.ts
@src/routes/+page.svelte
@src/routes/app/+page.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Clerk configuration for Phase 3 auth behavior</name>
  <files>src/lib/stores/clerk.svelte.ts</files>
  <action>Update Clerk load configuration to match D-03 and D-05: themed modal variables that align with the stone design system, `/app` as the post-auth destination, and `/` as the sign-out destination. Keep D-01 intact by using Clerk’s modal APIs instead of introducing any dedicated auth route.</action>
  <verify>
    <automated>rg -n "afterSignOutUrl: '/'|signInForceRedirectUrl: '/app'|appearance" src/lib/stores/clerk.svelte.ts</automated>
  </verify>
  <done>Clerk is configured for the landing-page-first flow and the modal can match the approved UI spec.</done>
</task>

<task type="auto">
  <name>Task 2: Build the landing page and shared auth UI components</name>
  <files>src/routes/+page.svelte, src/lib/components/landing/LandingHeader.svelte, src/lib/components/landing/FeatureCard.svelte, src/lib/components/layout/AppHeader.svelte, src/lib/components/layout/ShimmerGrid.svelte, src/lib/components/layout/UpgradePrompt.svelte, .planning/phases/03-auth-migration/03-02-SUMMARY.md</files>
  <action>Implement the landing page from D-04 and the UI spec, then create the reusable shell components Phase 3 needs later: `AppHeader` for D-12 and D-14, `ShimmerGrid` for D-06 and D-09, and `UpgradePrompt` for D-13. `AppHeader` must open Clerk sign-in/sign-up modals directly and only surface the profile entry when authenticated. As part of this plan, explicitly configure or verify the Clerk dashboard/provider setup required by D-02: Email+Password and Google OAuth must both be enabled. Record that verification in the plan summary so execution does not rely on an unstated dashboard assumption.</action>
  <verify>
    <automated>bun run lint && bun run format && bun run check && rg -n "Email \\+ password|Google OAuth" .planning/phases/03-auth-migration/03-02-SUMMARY.md</automated>
  </verify>
  <done>The public and in-app auth UI exists as reusable components, and the repo passes the required non-Convex verification suite.</done>
</task>

</tasks>

<verification>
Run `bun run lint && bun run format && bun run check`.
</verification>

<success_criteria>

- `/` is a real landing page with guest and sign-in CTAs.
- `AppHeader`, `ShimmerGrid`, and `UpgradePrompt` are available for later route wiring.
- Clerk sign-out goes to `/`, and auth still persists through Clerk session handling.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-auth-migration/03-02-SUMMARY.md`
</output>
