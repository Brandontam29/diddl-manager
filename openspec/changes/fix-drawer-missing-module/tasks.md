## 1. Dependency Management

- [x] 1.1 Add `@corvu/drawer` to `desktop-app/package.json` dependencies.
- [x] 1.2 Install dependencies in `desktop-app/` using `bun install` or `npm install`.

## 2. Verification

- [x] 2.1 Verify that the TypeScript error in `desktop-app/src/renderer/src/components/ui/drawer.tsx` is resolved.
- [x] 2.2 Run `bun run typecheck:web` (or equivalent) in `desktop-app/` to ensure no related errors.
