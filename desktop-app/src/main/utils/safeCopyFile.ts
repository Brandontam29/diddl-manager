// import fs, { copyFile } from 'fs/promises';

// const safeCopyFile = async (source: string, destination: string) => {
//     await copyFile(source, destination, fs.constants.COPYFILE_FICLONE);
//     return;
// };

// export default safeCopyFile;
