---
phase: 02-guest-list-mode
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/lib/components/lists/ColorPalette.svelte
  - src/lib/components/lists/CreateListDialog.svelte
  - src/lib/components/lists/DeleteListDialog.svelte
  - src/lib/components/lists/EditListDialog.svelte
  - src/lib/components/lists/ListCard.svelte
  - src/routes/app/+page.svelte
  - src/routes/app/lists/+page.svelte
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - LIST-11

must_haves:
  truths:
    - 'Guests can reach list management from a dedicated `/app/lists` route per D-03.'
    - 'The app entry page becomes a guest-accessible hub instead of a Clerk-only sign-in wall.'
    - 'List creation uses a modal with name and preset colors per D-01 and D-02.'
    - 'Rename, recolor, delete, and overview completion are available from the list overview page.'
    - 'CreateListDialog is reusable outside `/app/lists` via controlled open state and an `onCreated`-style completion hook.'
  artifacts:
    - path: 'src/lib/components/lists/CreateListDialog.svelte'
      provides: 'Reusable create-list modal with callback contract'
    - path: 'src/lib/components/lists/ListCard.svelte'
      provides: 'Overview card showing color, progress, count, and actions'
    - path: 'src/routes/app/lists/+page.svelte'
      provides: 'Dedicated guest list overview page'
    - path: 'src/routes/app/+page.svelte'
      provides: 'Guest-accessible entry hub linking to catalog and lists'
  key_links:
    - from: 'src/routes/app/lists/+page.svelte'
      to: 'src/lib/lists/listStoreContext.svelte.ts'
      via: 'shared store context'
      pattern: 'getListStoreContext'
    - from: 'src/lib/components/lists/CreateListDialog.svelte'
      to: 'src/lib/components/lists/ColorPalette.svelte'
      via: 'dialog form composition'
      pattern: 'ColorPalette'
    - from: 'src/lib/components/lists/ListCard.svelte'
      to: 'src/lib/components/catalog/CompletionBadge.svelte'
      via: 'overview progress badge reuse'
      pattern: 'CompletionBadge'
---

<objective>
Deliver the list overview experience and make guest mode discoverable from the app entry route.

Purpose: This closes the first user-facing slice of Guest List Mode: creating a guest list, editing it, deleting it safely, and seeing progress from a dedicated page. It also establishes the reusable list-creation dialog contract used later by catalog select mode.

Output:

- Reusable list CRUD dialogs and color palette
- `/app/lists` overview page
- `/app/+page.svelte` converted into a guest-accessible hub
  </objective>

<execution_context>
@/home/btam/.codex/get-shit-done/workflows/execute-plan.md
@/home/btam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-guest-list-mode/02-CONTEXT.md
@.planning/phases/02-guest-list-mode/02-RESEARCH.md
@.planning/phases/02-guest-list-mode/02-UI-SPEC.md
@src/lib/lists/listTypes.ts
@src/lib/lists/listStore.svelte.ts
@src/lib/lists/listStoreContext.svelte.ts
@src/lib/components/catalog/CompletionBadge.svelte
@src/routes/app/+page.svelte

<interfaces>
Locked overview decisions:
- D-01 create via dialog
- D-02 curated palette
- D-03 dedicated `/app/lists`
- D-04 destructive confirmation copy
- D-17 completion visible on overview
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the reusable list overview components and CRUD dialogs</name>
  <files>src/lib/components/lists/ColorPalette.svelte, src/lib/components/lists/CreateListDialog.svelte, src/lib/components/lists/EditListDialog.svelte, src/lib/components/lists/DeleteListDialog.svelte, src/lib/components/lists/ListCard.svelte</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-UI-SPEC.md
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-CONTEXT.md
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listTypes.ts
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/catalog/CompletionBadge.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/ui/dialog/index.ts
  </read_first>
  <action>
Create the overview-layer components:

- `ColorPalette.svelte`: 6-8 curated swatches from UI-SPEC, accessible as radio-like choices.
- `CreateListDialog.svelte`: create form with name + palette, controlled open state, and `onCreated(list)` or equivalent completion callback so other pages can continue work after creation.
- `EditListDialog.svelte`: rename/recolor dialog reusing the same form shape.
- `DeleteListDialog.svelte`: AlertDialog with list name and tracked-item count in destructive copy.
- `ListCard.svelte`: color bar, name, completion badge, item count, edit/delete actions, and navigation into `/app/lists/[id]`.

Use `lang="ts"` and Tailwind/shadcn patterns only.
</action>
<verify>
<automated>rg -n "Create List|onCreated|open =|Save Changes|Keep List|Delete" /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/CreateListDialog.svelte /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/EditListDialog.svelte /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/DeleteListDialog.svelte && rg -n "CompletionBadge|aria-label=\"Edit|aria-label=\"Delete|/app/lists/" /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/ListCard.svelte && rg -n "role=\"radio\"|aria-checked" /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/ColorPalette.svelte</automated>
</verify>
<acceptance_criteria> - The create dialog supports controlled open state and a completion callback - The edit dialog allows rename and recolor - The delete dialog includes the list name and item count - `ListCard.svelte` reuses `CompletionBadge` - All components use `lang="ts"`
</acceptance_criteria>
<done>Reusable list overview components exist, including a create dialog contract suitable for reuse from catalog select mode.</done>
</task>

<task type="auto">
  <name>Task 2: Wire `/app/lists` and replace the Clerk-only `/app` landing page with a guest-accessible hub</name>
  <files>src/routes/app/lists/+page.svelte, src/routes/app/+page.svelte</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/src/routes/app/+page.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listStoreContext.svelte.ts
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/ListCard.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/CreateListDialog.svelte
  </read_first>
  <action>
Create `src/routes/app/lists/+page.svelte` as the dedicated overview page:
- read the shared store via `getListStoreContext()`
- render page heading, create CTA, guest-limit guard, empty state, and responsive list-card grid
- reuse the dialog components from Task 1
- show completion percentage and item count for each list

Then replace the current Clerk-only sign-in wall in `src/routes/app/+page.svelte` with a guest-accessible entry hub that links to both `/app/catalog` and `/app/lists`. The page may still show authenticated affordances when a user exists, but guest users must be able to reach Guest List Mode without being blocked by a mounted sign-in widget.

Finish with:

```bash
bun run lint
bun run format
bun run check
```

  </action>
  <verify>
    <automated>rg -n "My Lists|Create List|getListStoreContext|ListCard|CreateListDialog" /home/btam/other-code/diddl-manager/website-svelte/src/routes/app/lists/+page.svelte && rg -n "/app/catalog|/app/lists|mountSignIn" /home/btam/other-code/diddl-manager/website-svelte/src/routes/app/+page.svelte && ! rg -n "mountSignIn" /home/btam/other-code/diddl-manager/website-svelte/src/routes/app/+page.svelte && cd /home/btam/other-code/diddl-manager/website-svelte && bun run lint && bun run format && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `/app/lists` exists and uses the shared list store
    - Guests can reach `/app/lists` from `/app`
    - `/app/+page.svelte` is guest-accessible and no longer mounts a sign-in wall for unauthenticated users
    - The overview page shows empty and populated states correctly
    - `bun run lint`, `bun run format`, and `bun run check` all succeed
  </acceptance_criteria>
  <done>Guest users can discover and use the list overview flow without authentication, and the app entry route now reflects that reality.</done>
</task>

</tasks>

<verification>
From `/app`, a guest can navigate into `/app/lists`, create a list, see its completion badge, rename it, and delete it through confirmation without first authenticating.
</verification>

<success_criteria>
LIST-01, LIST-02, LIST-03, and the overview portion of LIST-11 are satisfied through a dedicated list-management page and a guest-accessible app entry hub.
</success_criteria>

<output>
After completion, create `.planning/phases/02-guest-list-mode/02-03-SUMMARY.md`
</output>
