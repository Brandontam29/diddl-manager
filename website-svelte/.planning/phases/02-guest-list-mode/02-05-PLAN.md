---
phase: 02-guest-list-mode
plan: 05
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/lib/components/catalog/CatalogGrid.svelte
  - src/lib/components/catalog/CatalogItemCard.svelte
  - src/lib/components/lists/CatalogSelectToolbar.svelte
  - src/routes/app/catalog/+page.svelte
autonomous: true
requirements:
  - LIST-04

must_haves:
  truths:
    - 'The catalog page can enter a dedicated select mode and bulk-add selected catalog items to the guest list per D-06.'
    - 'Normal catalog browsing stays visually clean outside select mode per D-10.'
    - 'Guests with no list reuse the shared CreateListDialog contract from Plan 03 before selected items are added.'
    - 'Guests with an existing list add selected items directly through the shared ListStore.'
  artifacts:
    - path: 'src/lib/components/lists/CatalogSelectToolbar.svelte'
      provides: 'Catalog select-mode toolbar'
    - path: 'src/lib/components/catalog/CatalogItemCard.svelte'
      provides: 'Optional checkbox overlay only in select mode'
    - path: 'src/routes/app/catalog/+page.svelte'
      provides: 'Catalog select-mode state and bulk-add flow'
  key_links:
    - from: 'src/routes/app/catalog/+page.svelte'
      to: 'src/lib/components/lists/CreateListDialog.svelte'
      via: 'shared create-before-add flow'
      pattern: 'CreateListDialog|onCreated'
    - from: 'src/routes/app/catalog/+page.svelte'
      to: 'src/lib/lists/listStore.svelte.ts'
      via: 'bulk add selected catalog references'
      pattern: 'addCatalogItems'
---

<objective>
Finish Guest List Mode by adding catalog-side select mode and bulk add.

Purpose: Guests must be able to add items from the catalog view as well as from the list detail page. This plan adds that entry point without polluting normal browsing mode or duplicating list-creation UI.

Output:

- `CatalogSelectToolbar.svelte`
- Catalog card/grid selection support
- `/app/catalog` bulk-add flow using the shared store and create-list dialog contract
  </objective>

<execution_context>
@/home/btam/.codex/get-shit-done/workflows/execute-plan.md
@/home/btam/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-guest-list-mode/02-CONTEXT.md
@.planning/phases/02-guest-list-mode/02-RESEARCH.md
@.planning/phases/02-guest-list-mode/02-UI-SPEC.md
@src/routes/app/catalog/+page.svelte
@src/lib/components/catalog/CatalogGrid.svelte
@src/lib/components/catalog/CatalogItemCard.svelte
@src/lib/components/lists/CreateListDialog.svelte
@src/lib/lists/listStore.svelte.ts
@src/lib/lists/listStoreContext.svelte.ts

<interfaces>
Locked catalog decisions:
- D-06 dedicated select mode + bulk add
- D-07 no guest list picker needed because guests have one list max
- D-10 no persistent in-list marker in normal mode
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add select-mode presentation to the catalog component layer</name>
  <files>src/lib/components/lists/CatalogSelectToolbar.svelte, src/lib/components/catalog/CatalogItemCard.svelte, src/lib/components/catalog/CatalogGrid.svelte</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/catalog/CatalogItemCard.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/catalog/CatalogGrid.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-UI-SPEC.md
    - /home/btam/other-code/diddl-manager/website-svelte/.planning/phases/02-guest-list-mode/02-CONTEXT.md
  </read_first>
  <action>
Create `CatalogSelectToolbar.svelte` and extend the catalog card/grid pair so select mode is opt-in:
- toolbar shows selected count, add action, and exit action
- cards accept optional select-mode props and only show the checkbox overlay when select mode is active
- grid forwards select-mode props without owning selection state
  </action>
  <verify>
    <automated>rg -n "Add selected to list|Done Selecting|selectedCount" /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/CatalogSelectToolbar.svelte && rg -n "isSelectable|isSelected|onToggleSelected|Checkbox" /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/catalog/CatalogItemCard.svelte /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/catalog/CatalogGrid.svelte</automated>
  </verify>
  <acceptance_criteria>
    - Select mode UI exists without changing default catalog visuals
    - The checkbox overlay is absent when select mode is off
    - `CatalogGrid.svelte` remains presentation-only for selection state
  </acceptance_criteria>
  <done>The catalog component layer can render select mode without affecting normal browse mode.</done>
</task>

<task type="auto">
  <name>Task 2: Wire catalog select mode to the shared guest-list flow</name>
  <files>src/routes/app/catalog/+page.svelte</files>
  <read_first>
    - /home/btam/other-code/diddl-manager/website-svelte/src/routes/app/catalog/+page.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listStoreContext.svelte.ts
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/components/lists/CreateListDialog.svelte
    - /home/btam/other-code/diddl-manager/website-svelte/src/lib/lists/listTypes.ts
  </read_first>
  <action>
Update `src/routes/app/catalog/+page.svelte`:
- add select-mode state and selected natural-key tracking
- when a guest already has a list, bulk-add selected references through `addCatalogItems`
- when a guest has no list, open the shared `CreateListDialog`, wait for its `onCreated`-style callback, then add the selected references
- preserve existing query/range behavior and avoid any persistent "already in list" marker in normal mode

Finish with:

```bash
bun run lint
bun run format
bun run check
```

  </action>
  <verify>
    <automated>rg -n "selectMode|selectedCatalogKeys|Add selected to list|Done Selecting|getListStoreContext|CreateListDialog|onCreated|addCatalogItems" /home/btam/other-code/diddl-manager/website-svelte/src/routes/app/catalog/+page.svelte && cd /home/btam/other-code/diddl-manager/website-svelte && bun run lint && bun run format && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `/app/catalog` can enter select mode and bulk-add selected items
    - Guests with zero lists reuse the shared create-list dialog before add completes
    - Normal catalog browsing remains clean when select mode is off
    - `bun run lint`, `bun run format`, and `bun run check` all succeed
  </acceptance_criteria>
  <done>Catalog-side add-to-list is fully wired through select mode using the shared store and dialog contracts.</done>
</task>

</tasks>

<verification>
From `/app/catalog`, a guest can enter select mode, choose items, create a list if needed, and bulk-add the selected references without changing default browsing behavior.
</verification>

<success_criteria>
LIST-04 is satisfied from the catalog browsing flow using shared guest-list contracts instead of duplicated creation logic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-guest-list-mode/02-05-SUMMARY.md`
</output>
